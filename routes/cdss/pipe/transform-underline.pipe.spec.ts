/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TransformUnderlinePipe } from './transform-underline.pipe';

describe('Pipe: TransformUnderlinee', () => {
  it('create an instance', () => {
    let pipe = new TransformUnderlinePipe();
    expect(pipe).toBeTruthy();
  });
});
