import { TransformRiskPipe } from './transform-risk.pipe';

describe('TransformRiskPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformRiskPipe();
    expect(pipe).toBeTruthy();
  });
});
