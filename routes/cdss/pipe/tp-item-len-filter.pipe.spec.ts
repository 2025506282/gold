import { TpItemLenFilterPipe } from './tp-item-len-filter.pipe';

describe('TpItemLenFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TpItemLenFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
